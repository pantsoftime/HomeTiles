#include "src/types/switch/web_scripts.h"

// The editable JavaScript lives in src/web/assets/admin.js and is served as
// one precompressed, cacheable firmware asset.
void append_switch_scripts(String&) {}
